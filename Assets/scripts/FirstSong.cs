/*using UnityEngine;
using System.Collections;

public class FirstSong : MonoBehaviour {

    public class Ouroboros
    {
        AudioSource m_song =
        ulong m_delay;
        Ouroboros(ulong delay)
        {
            m_delay = delay;
        }

        void playSong()
        { m_song.Play(m_delay); }

        //delay is measured in seconds
        void playSong(ulong delay)
        { m_song.Play(delay); }

        void stopSong()
        { m_song.Stop(); }
    }

}*/
